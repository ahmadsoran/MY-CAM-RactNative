import { StyleSheet, Text, View } from "react-native";

const SignIN = () => {
  return (
    <View>
      <Text>Sign-in</Text>
    </View>
  );
};

export default SignIN;

const styles = StyleSheet.create({});
